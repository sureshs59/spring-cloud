package com.expensetracker.model;

/**
 * Expense categories — must match the GraphQL schema enum exactly.
 */
public enum Category {
    FOOD,
    TRANSPORT,
    ENTERTAINMENT,
    HEALTH,
    UTILITIES,
    SHOPPING,
    EDUCATION,
    OTHER
}
